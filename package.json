{
  "name": "npm-documentation",
  "repository": "npm/documentation",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "develop": "gatsby develop",
    "build": "gatsby build",
    "build-prefix-paths": "gatsby build --prefix-paths",
    "now-build": "npm run build",
    "serve": "gatsby serve",
    "serve-prefix-paths": "gatsby serve --prefix-paths"
  },
  "dependencies": {
    "gatsby": "^2.32.7",
    "gatsby-plugin-meta-redirect": "^1.1.1",
    "gatsby-theme-doctornpm": "^1.8.0",
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  }
}
